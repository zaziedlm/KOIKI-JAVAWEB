package org.koikifw.architecture;

/**
 * Relationship between the domain model and persistence representation.
 */
public enum PersistenceModel {

    /** Domain and persistence responsibilities share the same model. */
    SHARED
}
