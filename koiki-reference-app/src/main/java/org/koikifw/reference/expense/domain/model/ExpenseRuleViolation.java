package org.koikifw.reference.expense.domain.model;

/** Stable classifications for rejected expense-domain operations. */
public enum ExpenseRuleViolation {
    INVALID_CONTENT,
    INVALID_TRANSITION,
    SELF_DECISION
}

